# Claude Memory

## Rules for Claude

- 每次会话结束前（收到 `/commit end`）主动更新本文件：写入新识别到的偏好、项目进度变化、重要决策
- 中文交流，不删已有条目（除非用户明确要求）
- 上下文用量达到 70% 时提醒用户考虑 `/reset`
- 读取本文件后，直接按照其中的偏好和规则行事，不需要重新确认

## User Profile

- Name:
- Role:
- 背景/专长（帮助 AI 调整解释深度）：
- 常用工具/技术栈：

## Long-term Preferences

<!--
由 /commit end 自动写入，也可手动添加。
格式示例：
- 写作风格：不使用 emoji [行为归纳]
- 图片处理：只用 docx 嵌入，不用 Block API [明确规则]
- 竞品调研必须每家配截图 [场景：竞品调研]
-->

## Short-term Memory

<!--
当前阶段需要跨 session 记住的临时信息。
适合放：项目当前阶段、待确认事项、临时约定、环境状态。
不要放：密码、Token、API Key（这些存到安全的地方）。
-->

## Session Log

<!--
每次 /commit 追加一行：
[YYYY-MM-DD HH:MM] DONE 一句话描述 → 产出物路径
-->
